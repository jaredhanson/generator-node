/* global describe, it */

var pkg = require('..');
var expect = require('chai').expect;


describe('<%= name %>', function() {
  
  it('should work', function() {
    expect(1 + 1).to.equal(2);
  });
  
});
